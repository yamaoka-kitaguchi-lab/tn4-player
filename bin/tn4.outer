#!/bin/bash
DOCKER_IMAGE=ghcr.io/yamaoka-kitaguchi-lab/tn4-player:latest

docker run -it --rm \
  --network host \
  -v $(dirname $0)/../.secrets:/opt/tn4-player/.secrets:ro \
  ${DOCKER_IMAGE} tn4 $@
