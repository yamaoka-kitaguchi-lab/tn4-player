{% if interface.is_enabled %}
 no shutdown
{% else %}
 no switchport
 shutdown
{% endif %}

{% if interface.description %}
 description "{{ interface.description }}"
{% else %}
 no description
{% endif %}
