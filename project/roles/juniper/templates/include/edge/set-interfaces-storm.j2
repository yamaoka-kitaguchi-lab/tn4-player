{% if interface.is_physical and not interface.is_lag_member or interface.is_lag_parent and not interface.is_upstream %}
set interfaces {{ interface.name }} unit 0 family ethernet-switching storm-control "storm3M"
{% endif %}
